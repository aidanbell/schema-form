# Changesets

This repo uses [Changesets](https://github.com/changesets/changesets) for versioning and changelogs.

1. Make your changes
2. Run `pnpm changeset` and describe the change
3. Open a PR — release automation will version and publish when merged to `main`
